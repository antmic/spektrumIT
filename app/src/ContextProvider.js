import { createContext } from 'react';
export const ProfileContext = createContext([]);
export const profileIndexContext = createContext(0);
export const numberOfProfilesContext = createContext(0);
export const prevBtnDisabledContext = createContext(0);
